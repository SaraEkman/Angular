export class Animal {
  name: string;
  sort: string;
  weight: number;
  food: string;

  constructor(n: string, s: string, v: number, m:string) {
    this.name = n;
    this.sort = s;
    this.weight = v;
    this.food = m;
  }

}
